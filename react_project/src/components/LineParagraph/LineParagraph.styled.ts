import styled from 'styled-components';

export const StyledLineParagraph = styled.div`
  display: flex;
  align-items: center;

  span {
    display: block;
    font-size: var(--paragraph-med);
    font-weight: 600;
    line-height: 31.5px;
    color: var(--black-color);
    opacity: 0.6;
    margin-left: 32.46px;
  }
`;

export const Line = styled.div`
  border: 3px solid #1e1b1b;
  height: 112px;
`;
